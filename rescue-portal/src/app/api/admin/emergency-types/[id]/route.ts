import { NextResponse } from 'next/server'
import { createAdminClient, createClient } from '@/lib/supabase/server'
export async function PATCH(request: Request, context: { params: Promise<{ id: string }> }) { const client = await createClient(); const { data: { user } } = await client.auth.getUser(); if (!user) return NextResponse.json({ message: 'Sign in required.' }, { status: 401 }); const { data: profile } = await client.from('user_profiles').select('role, organization_id, is_active').eq('user_id', user.id).single() as any; if (!profile?.is_active || !['admin','super_admin'].includes(profile.role)) return NextResponse.json({ message: 'Admin access required.' }, { status: 403 }); const { id } = await context.params; const body = await request.json(); const admin = await createAdminClient() as any; const { data: existing } = await admin.from('emergency_types').select('organization_id').eq('id', id).single(); if (!existing?.organization_id || (profile.role !== 'super_admin' && existing.organization_id !== profile.organization_id)) return NextResponse.json({ message: 'Default types are protected.' }, { status: 403 }); const { data, error } = await admin.from('emergency_types').update({ name: body.name, icon: body.icon, color: body.color, is_active: body.is_active }).eq('id', id).select('*').single(); if (error) return NextResponse.json({ message: error.message }, { status: 400 }); return NextResponse.json({ emergencyType: data }) }
